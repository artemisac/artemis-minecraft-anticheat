package ac.artemis.core.v5.emulator.particle;

import ac.artemis.core.v5.emulator.particle.type.Particles;

public class BasicParticle extends Particle {
    public BasicParticle(Particles type) {
        super(type);
    }
}
